<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.login.mapper.UserMapper">
    <select id="findByUsername" parameterType="String" resultType="com.example.login.entity.User">
        SELECT id, username, password, created_time as createdTime FROM user_login.user WHERE username = #{username}
    </select>
    
    <select id="findAll" resultType="com.example.login.entity.User">
        SELECT id, username, password, created_time as createdTime FROM user_login.user LIMIT #{offset}, #{limit}
    </select>
    
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM user_login.user
    </select>
</mapper>
